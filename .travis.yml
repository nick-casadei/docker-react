language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t nickcas/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nickcas/docker-react npm run test -- --coverage
